pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Hello Build"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Hello deploy'
                sh 'cd demo/demo'
                sh 'mvn clean'
            }
        }
        stage('Test') {
            steps {
                echo 'Hello test'
            }
        }
    }
}